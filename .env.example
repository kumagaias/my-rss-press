# Environment Variables for Local Development
# Copy this file to .env if needed

# Note: GitHub MCP now uses `gh` CLI authentication
# No need to set GITHUB_PERSONAL_ACCESS_TOKEN here
# 
# Setup:
#   brew install gh
#   gh auth login
#
# Verify:
#   gh auth status

# AWS Credentials (if needed for local development)
# Prefer using AWS CLI configuration (~/.aws/credentials) instead
