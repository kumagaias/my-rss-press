#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run security check before commit
echo "🔒 Running security check..."
./.kiro/hooks/common/scripts/security-check.sh

# If security check fails, abort commit
if [ $? -ne 0 ]; then
  echo "❌ Security check failed. Commit aborted."
  echo "Please fix the security issues before committing."
  exit 1
fi

echo "✅ Security check passed. Proceeding with commit..."
