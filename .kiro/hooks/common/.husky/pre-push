#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-push checks..."

# テストを実行（ユニットテスト + セキュリティチェック + 脆弱性チェック）
echo "🧪 Running tests..."
make test

if [ $? -ne 0 ]; then
  echo "❌ Tests failed. Please fix the issues before pushing."
  exit 1
fi

echo "✅ All pre-push checks passed!"
