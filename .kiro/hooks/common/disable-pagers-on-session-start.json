{
  "name": "Disable CLI pagers (auto)",
  "description": "Automatically disable AWS CLI and Git pagers when session starts",
  "trigger": {
    "type": "onSessionCreate"
  },
  "action": {
    "type": "command",
    "command": "export AWS_PAGER=\"\" && export GIT_PAGER=\"\" && echo \"✅ Pagers disabled for this session\""
  }
}
