/**
 * Placeholder test file
 * This file exists to prevent vitest from failing when no other tests are present.
 * Remove this file once actual unit tests are added.
 */

import { describe, it, expect } from 'vitest';

describe('Placeholder', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });
});
